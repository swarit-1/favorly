# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# DB
asyncpg>=0.29.0

# Graph metrics
networkx>=3.2

# LLM / embeddings client (OpenAI-compatible)
openai>=1.30.0

# Validation
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Env
python-dotenv>=1.0.0

# NOTE: scikit-learn/numpy were listed for weight learning that was never
# written -- nothing imports them, and they pulled in ~163MB (scipy included),
# which does not fit in a serverless function. Add them back with the code
# that needs them.
